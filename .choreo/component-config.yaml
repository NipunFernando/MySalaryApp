apiVersion: core.choreo.dev/v1beta1
kind: ComponentConfig
spec:
  inbound:
    - name: Express Backend
      port: 5000  # Adjust this to match your Express.js port
      type: REST
      networkVisibility: Public  # You can change this to Project or Organization if needed
      context: /api  # Adjust this to match your API base path
      schemaFilePath: openapi.yaml  # You'll need to create this OpenAPI specification file